<section class="about">
  <h4>ABOUT</h4>
  <h2 class="highlight">Igniting Innovation</h2>
  <p class="about_text">
    Our strength lies in the collaboration between our talented teams and
    esteemed clients. We cultivate a workplace that champions innovation,
    promotes peak performance, and guarantees client satisfaction.
  </p>
</section>

<style>
  @layer components {
    .about {
      display: flex;
      flex-direction: column;
      align-items: center;
      text-align: center;
      gap: 1rem;
      background-color: var(--color-neutrals-50);
      padding: 70px;
      overflow: hidden;
    }

    .about_text {
      width: 40%;
      margin: 0 auto;
      text-align: center;
    }

    .highlight {
      position: relative;
      display: inline-block;
      z-index: 1;
    }

    .highlight::after {
      content: "";
      position: absolute;
      left: 0px;
      bottom: 2px;
      width: calc(100% + 900px);
      height: 6px;
      background: var(--color-yellow-500);
      z-index: -1;
    }

    @media (max-width: 1024px) {
      .about_text {
        width: 90%;
        margin: 0 auto;
      }

      .highlight::after {
        content: "";
        position: absolute;
        left: 0px;
        bottom: 2px;
        width: calc(100% + 900px);
        height: 5px;
        background: var(--color-yellow-500);
      }
    }
  }
</style>
