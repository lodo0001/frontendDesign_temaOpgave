<section class="about">
  <h4>TEAM</h4>
  <h2 class="highlight">The Consultors</h2>
  <p class="about_text">
    Our success stems from the synergy between dedicated teams and our valued
    clients. We prioritize a company culture that fosters exceptional
    productivity, ensures customer satisfaction.
  </p>
</section>

<style>
  @layer components {
    .about {
      display: flex;
      flex-direction: column;
      align-items: center;
      text-align: center;
      gap: 1rem;
      background-color: var(--color-neutrals-50);
      padding: 70px;
      overflow: hidden;
    }

    .about_text {
      width: 40%;
      margin: 0 auto;
      text-align: center;
    }

    .highlight {
      position: relative;
      display: inline-block;
      z-index: 1;
    }

    .highlight::after {
      content: "";
      position: absolute;
      right: 0px;
      bottom: 2px;
      width: calc(100% + 900px);
      height: 6px;
      background: var(--color-yellow-500);
      z-index: -1;
    }
  }
</style>
